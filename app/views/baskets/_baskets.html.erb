<div class="list-group">
<% baskets.each do |basket|%>
    <a class="list-group-item list-group-item-action" href= <%= basket_path(basket.id)%> >  Receipt <%= basket.id%> | <%= basket.updated_at%> </a>
    <%end%>
</div>